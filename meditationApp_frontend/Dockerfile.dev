FROM node:20-alpine

WORKDIR /app

COPY package*.json ./
RUN npm install

COPY . .

EXPOSE 5173

# Escribe las vars VITE_* al .env para que Vite las exponga al browser
CMD sh -c 'echo "VITE_API_URL=${VITE_API_URL:-http://localhost:3000/api}" > .env && npm run dev -- --host'
